server:
  port: 8080

security:
  jwt:
    token:
      expire-length: 3600000
      secret-key: dummy-secret-key-should-be-long-enough-123456
  admin:
    password: dummy-admin-password

cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket: dummy-code-l-bucket
    credentials:
      access-key: dummy-access-key
      secret-key: dummy-secret-key

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, prometheus
  metrics:
    enable:
      all: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://3.34.185.239:13306/codeltemp
#    url: jdbc:mysql://localhost:3306/codel
    username: admin
    password: codecode0221

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  
  flyway:
    enabled: false

springdoc:
  override-with-generic-response: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace